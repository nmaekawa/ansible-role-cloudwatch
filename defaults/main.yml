---

# managed policy and trust policy document filename must be prefixed
# by this `permission_name`
permission_name: "ec2_cloudwatch_put_read_list"
# folder where managed policy and trust policy document files are located
policy_document_path: "{{ role_path }}/files"

# user that cloudwatch cronjobs run as
cloudwatch_user: "root"


